# This script needs to read two numbers and do a sum

number1 = float(input('Type a number: '))
number2 = float(input('Type another number: '))

print('The sum between these numbers is equals to ', number1 + number2)